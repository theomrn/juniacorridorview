server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Proxy API → .NET backend (conserve le préfixe /api)
    location /api/ {
        client_max_body_size 0;
        proxy_pass         http://api:5078/api/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Fichiers statiques uploadés (images panoramas)
    location /previews/ {
        proxy_pass http://api:5078/previews/;
    }

    location /images/ {
        proxy_pass http://api:5078/images/;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
